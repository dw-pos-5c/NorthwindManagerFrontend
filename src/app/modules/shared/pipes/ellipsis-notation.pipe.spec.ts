import { EllipsisNotationPipe } from './ellipsis-notation.pipe';

describe('EllipsisNotationPipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsisNotationPipe();
    expect(pipe).toBeTruthy();
  });
});
